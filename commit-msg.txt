Cleanup + one-click installers for APK and Windows

- Move make-icons.mjs into scripts/, drop commit-msg cruft
- Add NSIS installer (installer.nsi): installs to %LOCALAPPDATA%,
  checks/installs Node.js, runs setup, Start Menu + desktop shortcuts,
  Add/Remove Programs entry + uninstaller
- CI now builds BOTH installers: Android APK and Windows .exe,
  renamed CmdRemote-Android-<ver>.apk / CmdRemote-Windows-<ver>.exe,
  attached to tagged releases
- README: prominent Download section linking to latest release

Co-authored-by: CommandCodeBot <noreply@commandcode.ai>
